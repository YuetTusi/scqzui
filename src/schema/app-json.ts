
/**
 * 应用配置
 */
export interface AppJson {
    /**
     * 是否开启默认关键词模版
     */
    useDefaultTemp: boolean,
    /**
     * 是否开启关键词验证
     */
    useKeyword: boolean,
    /**
     * 是否开启文档验证
     */
    useDocVerify: boolean
}