import styled from 'styled-components';

export const CaseListBox = styled.div`

    width: 400px;
`;

export const NowrapText = styled.a`
    cursor: pointer;
    display: inline-block;
    max-width: 220px;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
    vertical-align: middle;
`;