import styled from 'styled-components';

export const AndroidIconBox = styled.span`
    color:${props => props.theme['text-color']};;
`;

export const AppleIconBox = styled.span`
    color:${props => props.theme['text-color']};
`;